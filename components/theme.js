import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  fonts: {
    heading: `'Poppins', 'devanagari','latin','latin-ext'`,
    body: `'Poppins', 'devanagari','latin','latin-ext'`,
  },
})

export default theme